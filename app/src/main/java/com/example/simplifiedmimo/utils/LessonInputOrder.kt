package com.example.simplifiedmimo.utils

// this enum class has the possible values for the order (the position) of the input within the contents of a lesson
enum class LessonInputOrder {
    BEGINNING, // the input is at the beginning
    MIDDLE,   // the input is in the middle
    END      // the input is at the end
}